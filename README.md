# Character-Counter
A responsive Character Counter web application built using HTML, CSS, and JavaScript. It displays the number of characters typed, remaining characters, and shows a warning when the 200-character limit is reached. Simple, interactive, beginner-friendly, and easy to customize
